# 역할분담 — 통합에서 제출까지 (완성본 스프린트)

> 기준일: 2026-07-30 · 인원 4명 · 상위 Task `.github#6`(v3 수직 슬라이스) → **M3 잔여 + M4 제출**
> 직전 사이클 결과: **v3 수직 슬라이스 4갈래 전부 브랜치 올라옴** (app#7 jch / app#8 kys / ai#30 pjh / ai#31 ljs).
> 이번 사이클 목표: **네 갈래를 하나로 합쳐 종로 1코스를 끝까지 돌리고, 제출물(빌드·시연영상·문서)까지 낸다.**
>
> 기준 자료: `최종_시나리오/`(3층 스키마·상태그래프·이탈대응·힌트) · `최종_UI/종로의 기억석 v3` · `개발계획.md`(M3·M4).
> 지난 사이클 문서는 git 히스토리에 있음(`WEEKLY-PLAN.md` @ 07-16).

---

## 0. 지금 어디까지 왔나

```
[백엔드 파이프라인] ✅ 선택(위시·비인기·식음/예산)  ✅ 경로분기(route_tree)  ✅ 식음 실데이터
[최종 스키마]       ✅ DTO seam(동기·전략·액션·grants/requires·힌트)  ✅ 생성층(#30)
[최종 UI v3]        ✅ 전반부(#7)  ✅ 후반부·모달·상태연동(#8)  ✅ 콘텐츠·QA·도감설계(#31)
        │
        ▼  ← 이번 사이클 = 여기. 네 갈래가 **각자 브랜치에 따로 있다.**
[통합]  ⬜ 머지·충돌 해소·E2E 에러      [완성]  ⬜ M3 잔여      [제출]  ⬜ M4
```

**이번 사이클의 성격 = "합치는 주".**
지난 사이클은 각자 병렬로 넓혔다. 병렬로 넓힌 것은 **반드시 겹친다** — 그 겹침을 정리하지 않으면
데모 당일에 터진다. 그래서 1단계는 기능 추가가 아니라 **합치기**다.

---

## 1. 통합 리스크 — 미리 찍어둔 충돌 지점

> 합치기 전에 이걸 먼저 합의해야 머지가 싸움이 안 된다.

### 🔴 C1. v3 전반부 화면이 **두 벌** 존재 (최우선)

`app#8`의 `quest_journey_screen.dart`는 v3 시안을 **한 화면 상태머신**으로 재현해서
`setup · map · gps · summon · dialogue · quiz · order`를 **이미 포함**한다.
이건 `app#7`(jch) 담당 범위와 정확히 겹친다. 합치면 같은 7화면이 두 벌이 된다.

**해소 규칙(합의안)**: **jch의 개별 화면을 정본으로, `quest_journey_screen`은 "여정 오케스트레이터"로 축소.**

| | 역할 |
|---|---|
| jch | 각 단계를 **독립 위젯/화면**으로 (`SetupScreen`·`HonbulRadarScreen`·`ArSummonScreen`·`DialogueScreen`…) |
| kys | 오케스트레이터는 **단계 전환 + 상태(조각·단서·플래그) + 게이팅**만 들고, 각 단계는 jch 위젯을 **호출**. 중복 구현 삭제 |

이유: 상태·게이팅은 한 곳에 모으고 화면은 재사용 조각으로 — 멀티·도감에서 그대로 재사용된다.

### 🔴 C2. 혼불 레이더 중복

`honbul_home_screen.dart`(kys, 메인 지도 레이더)와 `app#7`의 **혼불 레이더 GPS**가 겹친다.
→ **레이더 위젯 1개로 통일**(`widgets/honbul_radar.dart`), 메인·인퀘스트 양쪽에서 파라미터로 재사용. 담당 jch, 리뷰 kys.

### 🟡 C3. 앱 진입점 변경

`app#8`에서 로그인 후 첫 화면을 `MainShell`(5탭) → `HonbulHomeScreen`으로 바꿨다.
→ **전원 합의 필요.** 유지하면 5탭은 '나' 탭 하위로 들어간다. 첫 회의 안건.

### 🟡 C4. 노드 스키마 — 파일 충돌은 없고 **계약 충돌**이 남는다

`ai#30`이 실제로 내리는 JSON이 앱이 기대하는 필드명·모양과 어긋나면 **조용히 폴백**해서 빈 화면이 된다.
파일이 안 겹치니 git은 알려주지 않는다. → **계약 테스트로 잡는다**(3절 kys ⑤). `시나리오구조화.md` 6-1 노드1 JSON이 정본.

### 🟡 C5. 공용 파일 동시 수정

`lib/theme.dart` · `lib/widgets/*` · `app/api/schemas.py` — 3명 이상이 만진다.
→ **규칙: 공용 파일은 추가만(append-only), 기존 값 변경은 PR 본문에 명시.**

### 🟢 C6. CI가 **없다**

현재 네 레포 어디에도 워크플로가 없어서 깨진 코드가 그대로 dev에 들어간다. → 이번 사이클에 kys가 깐다.

---

## 2. 3단계 로드맵

| 단계 | 기간 | 목표 | 나가는 것 |
|---|---|---|---|
| **P1 통합·안정화** | 1주 | 4갈래 머지, C1~C6 해소, 종로 1코스 완주 | dev에서 도는 통플레이 빌드 |
| **P2 완성 기능** | 2주 | M3 잔여(공용화·도감/칭호·맞춤생성·멀티) | 기능 완결 빌드 |
| **P3 QA·제출** | 1주 | 현장테스트·버그픽스·시연영상·제안서 | **제출물 일체** |

> **P1을 건너뛰고 P2로 가지 않는다.** 안 합쳐진 상태에서 기능을 더 얹으면 충돌이 제곱으로 늘어난다.

---

## 3. P1 — 통합·안정화 (1주)

### 🟦 김예슬 (kys) — **통합 오너** ⭐이번 사이클 신규 담당

- **① 머지 순서 집행** — 의존 낮은 것부터. 각 단계마다 통플레이 확인 후 다음으로:
  1. `app#8 app-v3-back/kys/v1` — 디자인토큰·AR위젯·DTO seam (**남들이 기다리는 seam**)
  2. `ai#30 node-schema-gen/pjh/v1` — 생성층(앱이 소비할 데이터)
  3. `ai#31 content-qa-design/ljs/v1` — 콘텐츠·힌트 문구
  4. `app#7 app-v3-front/jch/v1` — **C1 해소와 함께**
- **② 충돌 해소 주재** — C1·C2는 코드로 직접 정리(오케스트레이터 축소·중복 삭제), C3~C5는 합의 후 반영.
- **③ E2E 에러 정리** — 종로 1코스 처음~엔딩 통플레이를 돌려 터지는 것 전부 잡고 이슈로 등록·배분.
- **④ CI 구축**(C6) — PR마다 자동으로:
  | 레포 | 검사 |
  |---|---|
  | app | `flutter analyze`(에러 0) + `flutter test` |
  | ai | `pytest` + `ruff` |
  | server | `npm run build` + `npm test` |
  → 목적은 **깨진 채로 dev에 못 들어오게** 하는 것. 이게 있으면 "에러 확인"이 사람 일에서 기계 일로 넘어간다.
- **⑤ 계약 테스트**(C4) — AI가 낸 **실제 응답 JSON**을 앱 모델에 통과시키는 테스트. 6-1 노드1을 픽스처로 고정 → 필드명이 어긋나면 **CI가 빨개진다.**
- **브랜치:** `integration/kys/v1` · `ci/kys/v1`

### 🟨 정찬희 (jch)
- **C1 해소 본체** — 전반부 단계를 독립 위젯/화면으로 분리해 오케스트레이터가 호출 가능하게. 중복 제거는 kys와 짝으로.
- **C2** — 혼불 레이더 위젯 1개로 통일(메인·인퀘스트 재사용).
- 자기 브랜치 머지 충돌 해소 + 통플레이 중 나온 앱 버그 픽스.
- **브랜치:** `app-v3-front/jch/v2`

### 🟧 박준형 (pjh)
- **생성층 실데이터 검증** — 종로 4노드가 스키마 v1.1 필드(motivation/strategy/actions/grants/requires/clue/hint)를 **빠짐없이** 채우는지 직접 확인. 빈 필드는 앱이 조용히 폴백하므로 화면만 봐선 모른다.
- 동기↔전략 제약 위반 리롤 회귀 테스트.
- 정답유출·어미·환각 자동체크를 **CI에 물릴 형태**로 정리(kys와 인터페이스 합의).
- **브랜치:** `node-schema-gen/pjh/v2`

### 🟩 이지선 (ljs)
- **힌트 문구 3단 슬롯 채우기** — 종로 4노드 × H1/H2/H3. 타이밍 코드는 이미 붙어 있어 **문구만 넣으면 즉시 동작**(`hint_ladder.H1/H2/H3`, `"slot"`은 미채움으로 인식).
- 종로 정답지 baseline 확정 + LLM QA 4항목 리포트 1회.
- **도감/단서함 카드 규격** 전달 → 앱 구현(kys)이 이것 때문에 대기 중.
- **브랜치:** `content-qa-design/ljs/v2`

**P1 DoD** — ☐ 4갈래 dev 머지 ☐ C1~C3 해소 ☐ CI 초록 ☐ **종로 1코스 처음~엔딩 통플레이 성공(영상/스크린샷)**

---

## 4. P2 — 완성 기능 (2주 · M3 잔여)

> P1에서 합친 토대 위에 **아직 없는 것**만 채운다. 레포 실측 기준으로 빈 곳만 적었다.

### 🟦 김예슬
- **도감·칭호·보상 시스템** — 앱 화면(ljs 설계 기반) + 서버 API. 조각·단서·플래그가 이미 영속되니 그 위에 얹는다.
- **공용 시나리오 풀** — 등록 게이트·평점·완주율 (`dokkaebi-server`)
- **실시간 멀티 마감** — `party.gateway.ts`는 현재 골격만. 4인 파티 조각 동기화·랭킹(Redis 원자처리) 채우기.
- 통합·리뷰·CI 유지

### 🟨 정찬희
- **파티·멀티 UI** — 초대·파티창·실시간 조각 공유·인게임 채팅
- 위시 라이브러리 화면(ljs 설계 기반)
- 🔄 **서버 API 일부 인수**(개발계획 M3 재조정) — 도감/칭호 API 등으로 kys 부하 분산

### 🟧 박준형
- **위시 기반 맞춤 생성**(앵커+샛길) — 설계 노트는 있음, 구현 차례
- **RAG 검색 품질** — `retrieve.py`에 "배선까지 완료, top-k·재랭킹·신뢰도·저신뢰 재검색은 TODO(박준형)"로 남아 있다. 이번에 채운다.
- 공용화 자동검수(규칙·텍스트 필터), eval 회귀셋

### 🟩 이지선
- **RAG 지식베이스** — 청킹·임베딩·인덱싱 데이터 품질(pjh 검색 전략과 짝)
- NPC 페르소나 시드 확장(종로 → 2지역)
- 혼잡도 → 비인기지 **보상 가중치 밸런싱** 확정
- 디자인: 파티·랭킹·도감/칭호 화면

**P2 DoD** — ☐ 위시→맞춤생성→공용등록 흐름 완결 ☐ 4인 파티 합동 복원 동작 ☐ 도감·칭호 표시

---

## 5. P3 — QA·제출 (1주 · M4)

| 담당 | 할 일 |
|---|---|
| **전원** | 종로 **현장** 통합 테스트(GPS 반경·AR·네트워크), 버그 픽스 |
| 김예슬 | 릴리스 관리(`dev`→`main`), 빌드 배포, 성능 점검(LLM 캐시·지연), 최종 회귀 |
| 정찬희 | **시연 영상**(솔로+멀티) 촬영·편집, 테스트 기기 빌드 |
| 박준형 | **데이터 활용·AI 파이프라인** 발표자료 (EDA·생성·RAG·eval 수치) |
| 이지선 | **제안서·발표자료** 반영, 스크린샷·에셋 정리 |

**P3 DoD** — ☐ 제출물 일체(시연영상·발표자료·제안서·빌드) ☐ `main` 릴리스 태그

---

## 6. 규칙 (이번 사이클 추가분)

1. **P1 기간엔 새 기능 브랜치를 따지 않는다.** 합치기가 끝나야 P2 시작 (충돌 제곱 방지).
2. **머지는 kys 순서대로.** 순서를 건너뛰면 seam 없이 합쳐져 되돌리기가 커진다.
3. **CI 초록 아니면 머지 금지** (P1 ④ 이후 적용).
4. 공용 파일(`theme.dart`·`widgets/*`·`schemas.py`)은 **추가만**, 기존 값 변경 시 PR 본문 명시.
5. 브랜치 `<기능>/<이름>/<버전>`, `dev`로 PR, 1인 리뷰 승인 — 기존 유지.
6. **막히면 24시간 안에 말한다.** P1은 서로 물려 있어 한 명이 조용히 막히면 전원이 선다.

---

## 7. 한 줄 요약

> 지난 사이클 = 네 갈래로 **넓혔다.**
> **이번 사이클 = 넷을 하나로 합치고(P1), 빈 곳을 채우고(P2), 제출한다(P3).**
> 합치기 오너는 김예슬 — 머지 순서·충돌 해소·E2E 에러·CI·계약 테스트.
> 최우선 리스크는 **C1(v3 전반부 화면 두 벌)** — 첫 회의에서 정본을 정하고 시작한다.
