# 이번 주차 역할분담 — 최종 시나리오·UI 구현 (종로 v3 수직 슬라이스, **UI 우선**)

> 기준일: 2026-07-15 · 대상: `dokkaebi-app`(주) + `dokkaebi-ai`(생성층) · 상위 Task `.github#6`
> 지난 주 결과: **① 선택 hook 3종 + ② 경로 분기(#24) 전부 dev 머지 완료** · 식음 실데이터(#28) 머지.
> 이번 주 목표: **"종로 1코스가 새 스키마 + v3 화면으로 처음~엔딩까지 도는 수직 슬라이스"** — **UI 우선.**
>
> 기준 자료: `최종_시나리오/`(3층 스키마·상태그래프·이탈대응·힌트) · `최종_UI/종로의 기억석 v3`.
> 지난 주 문서는 git 히스토리에 있음(`WEEKLY-PLAN.md` @ 07-05).

---

## 0. 지금 어디까지 왔나 (스냅샷)

```
[백엔드 파이프라인]  ✅ ① 선택(위시·비인기·식음/예산)  ✅ ② 경로 분기(route_tree)  ✅ 식음 실데이터
        │
        ▼   ← 이번 주 = 여기서부터 "최종 스키마 + v3 화면"으로 갈아끼우는 주
[최종 스키마]  ⬜ 동기 M1–M9 · 전략 S1–S7 · 액션 원자 · grants/requires/flags · 단서체인 · 힌트 사다리
[최종 UI v3]  ⬜ SETUP→MAP→혼불레이더→AR→대화→퀴즈→지령→사냥→사진→발자국→카페→인사동→세종→엔딩 + 모달
```

**이번 주의 성격 = "기획을 화면으로 세우는 주".**
지난 주까지는 백엔드 파이프라인. 이번 주는 **v3 화면을 앱에 먼저 세우고**, AI 스키마는
*화면이 필요로 하는 필드부터* 채워 맞춘다(목/더미로라도 종로 1코스가 끝까지 돌게).

---

## 1. 사람별 분담

### 🟨 정찬희 (jch) — 앱 v3 UI 전반부 + 위시 입력  · `dokkaebi-app#7`
- **위치:** v3 도입~지령 화면 (플레이 진입부)
- 화면: **SETUP**(새 여정 꾸리기) · **MAP**(챕터 지도) · **혼불 레이더 GPS**(나침반+거리계+근접 진동) · **AR 소환** · **대화**(분기 A/B/C) · **퀴즈** · **지령**
- **위시 입력 UX**(검색→선택 칩→지도핀/태그) → generateScenario 배선(no_meals 포함), 반경밖 경고
- 목/더미로 전반부 플레이 + 스크린샷
- **브랜치:** `app-v3-front/jch/v1`

### 🟦 김예슬 (kys) — 앱 v3 UI 후반부·모달 + 데이터/상태 연동 + 서버 seam  · `dokkaebi-app#8`
- **위치:** v3 후반부 + **통합 오너**(스키마↔화면)
- 화면: **사냥·사진·발자국·카페·인사동·세종·엔딩** + **보상/힌트/컬렉션 모달**
- **상태 연동:** 조각/단서/플래그 인벤토리, `requires`(soft/hard) 게이팅, 피날레 구조적 종료, 엔딩 분기, `route_tree` 갈림길 렌더
- **힌트 사다리 H1–H3** 타이밍(코드) + 이탈대응 D1–D4(안내/부분인지) 최소 동작
- **서버 DTO seam:** node_sequence 새 필드(motivation/strategy/actions/grants/requires/clue/hint) 앱 모델 반영 — jch와 조율
- **브랜치:** `app-v3-back/kys/v1`

### 🟧 박준형 (pjh) — AI 노드 스키마 생성층  · `dokkaebi-ai#30`
- **위치:** 최종 3층 스키마의 **생성층**(화면 필요 필드 우선)
- 8-B NPC 합성 + **동기 M1–M9 매핑** + 동기→전략(S1–S7) 제약표 검증
- 전략 → **액션 원자 시퀀스** 컴파일(기존 mission 확장)
- **화면 필요 필드 우선:** intro·choices·quiz 문구·order(지령)·hint·clue·grants → 앱이 소비 가능한 node_sequence
- S7 PATRONIZE(식음, 본인 hook) + 이중경로 D6
- **브랜치:** `node-schema-gen/pjh/v1`

### 🟩 이지선 (ljs) — 콘텐츠 정답지 + LLM QA + 힌트 문구 + 도감/단서함 설계  · `dokkaebi-ai#31`
- **위치:** 수직 슬라이스의 **콘텐츠 품질·기준**
- **종로 정답지**(노드1~4·사이드) baseline + **LLM QA 4항목**(유창성/일관성/참신성/창의성) + 자동체크(정답유출·어미·환각)
- **힌트 사다리 H1–H3 문구 슬롯**(타이밍=코드는 kys)
- **도감/컬렉션·단서함 UI 설계**(v3 기준, `단서설계규칙.md`) → 앱 구현(kys) 기반 제공
- 보상 밸런싱 데이터(비인기·쿠폰) 갱신
- **브랜치:** `content-qa-design/ljs/v1`

---

## 2. 의존 순서 & 충돌 방지

```
UI 우선 →  jch(전반부) ┐
                       ├─ 목/더미로 병렬 착수 (스키마 미완이어도 화면 먼저)
           kys(후반부) ┘        │
                                ▼  스키마 필드 정해지면 데이터 연동
   pjh(생성층 필드) ──► 서버 DTO seam(kys↔jch) ──► 실데이터 바인딩
   ljs(정답지/QA) ──► 콘텐츠·힌트 문구 공급 + 도감 설계 → kys 구현
```

**규칙**
1. **앱 두 사람(jch/kys)은 화면부터** — 스키마 미완이어도 목/더미로 진행(UI 우선 원칙).
2. 앱 분할: **jch=전반부(도입~지령) / kys=후반부·모달·데이터/상태**. 겹치는 공통 위젯은 `widgets/`에서 공유.
3. AI 두 사람(pjh/ljs)은 **화면이 요구하는 필드·콘텐츠부터** 공급 — 앱 스크린이 소비 명세.
4. cross-talk = **서버 DTO seam**(node_sequence 새 필드) → kys가 조율.
5. 브랜치 규칙 유지: `<기능>/<이름>/v1`, main/dev 직접 수정 ❌, PR 1인 리뷰 승인.

---

## 3. 한 줄 요약

> 지난 주 = 백엔드 파이프라인(선택·분기·식음) 마감.
> **이번 주 = 최종 v3 화면을 앱에 세우고(UI 우선), 종로 1코스를 새 스키마로 끝까지 돌리는 주.**
> 앱 jch(전반부)+kys(후반부·통합), AI pjh(생성층)+ljs(정답지·QA·도감 설계). 상위 `.github#6`.
