# 역할분담 — 9월 제출까지 4주 스프린트

> 기준일: 2026-07-30 · 인원 4명 · **제출 = 9월 초** (버퍼 약 1주)
> 직전 사이클 결과: **v3 수직 슬라이스 4갈래 전부 브랜치 올라옴** (app#7 jch / app#8 kys / ai#30 pjh / ai#31 ljs).
>
> 기준 자료: `최종_시나리오/` · `최종_UI/종로의 기억석 v3` · `개발계획.md`(M3·M4) · `아키텍처_파이프라인.md`(9절 배포).
> 지난 사이클 문서는 git 히스토리에 있음(`WEEKLY-PLAN.md` @ 07-16, 07-30 1차).

---

## 0. 4주 캘린더

| 주 | 기간 | 목표 | 끝에 나오는 것 |
|---|---|---|---|
| **W1** | 08/01~08/07 | **통합 + 기능 + 로컬 풀스택 실동작** | 내 컴퓨터에서 처음~엔딩이 **실서비스로** 도는 상태 |
| **W2** | 08/08~08/14 | **실시간 멀티** (4인 파티·조각 동기화·랭킹) | 4인 합동 복원 동작 |
| **W3** | 08/15~08/21 | **배포** | 외부에서 접속되는 서비스 + 설치 빌드 |
| **W4** | 08/22~08/28 | **QA·제출물** | 시연영상·발표자료·제안서·릴리스 |
| 버퍼 | 08/29~09/초 | 예비일 | 제출 |

**W1이 이 스프린트의 승부처다.** 통합(P1)과 기능(P2)을 한 주에 밀어넣는다.
단, **"더 빨리 일해서" 되는 게 아니라 범위를 잘라서** 넣는다 — 잘라낸 목록은 1절에 명시.

---

## 1. W1에 넣기 위해 자른 것 (합의 필요)

> 1주 압축은 속도가 아니라 **범위 결정**이다. 아래를 자르지 않으면 W1은 못 끝난다.

| 자른 것 | 왜 | 어디로 |
|---|---|---|
| **C1 전면 리팩터**(v3 전반부 7화면 재구조화) | 내부 구조 정리라 **제출물에 보이는 값이 0**. 1주에 하면 통플레이가 깨질 위험만 큼 | 제출 후로 이월. W1엔 **데모 경로 정본만 선택**(2절 C1) |
| **실시간 멀티** | `party.gateway.ts`가 골격만 → 새로 짜야 함(1인×1주+) | **W2 전용 주차로 분리** ✅확정 |
| 공용 시나리오 풀의 **평점·완주율** | 등록·목록만 있으면 데모는 성립 | W4 여유 시 / 제안서엔 설계로 |
| 2지역 확장(페르소나·콘텐츠) | 심사 포인트는 종로 1코스의 완성도 | 제안서 "확장 계획"으로 |

**W1에 남기는 것** = 통합 + 도감·칭호 + 맞춤생성 + RAG 품질 + 힌트 문구 + **로컬 풀스택 실동작**.

---

## 2. 통합 충돌 지점 — **실측 결과 반영 (07/30 머지 완료)**

> 예측 6건 중 실제로 터진 건 하나뿐이고, 예측 못 한 게 하나 터졌다. 아래는 실제 결과.

| | 예측 | 실제 |
|---|---|---|
| C1 화면 두 벌 | 🔴 최우선 | **발생 안 함** — jch가 개별 화면을 만들지 않았고 오케스트레이터가 이미 커버. 리팩터 보류(제출 후) |
| C2 레이더 중복 | 🔴 | 남아 있음 — jch 담당, W1 잔여 |
| C3 진입점 변경 | 🟡 | 이의 없이 머지됨. 유지 |
| C4 계약 충돌 | 🟡 | **계약 테스트로 검증 통과** — pjh 실제 출력이 앱 DTO에 그대로 파싱됨 |
| C5 공용 파일 | 🟡 | 충돌 0 (git 자동 병합) |
| C6 CI 없음 | 🟢 | 아직 없음 — W1 잔여 |
| **C7 SDK 버전 분열** | ❌ 예측 못 함 | 🔴 **실제로 dev를 깨뜨린 유일한 건** (아래) |

### 🔴 C7. Flutter SDK 버전 분열 — 실제로 터진 것
 
`ThemeData.cardTheme` 파라미터 타입이 3.27에서 `CardTheme` → `CardThemeData`로 바뀌었다.
팀 SDK가 **3.24.3(kys) ↔ 3.44.1(jch)** 로 갈려 있어 **어느 쪽을 써도 반대편 빌드가 깨진다.**
  
해소(app#11): 업그레이드를 강요하지 않고 **버전 의존 API를 안 쓴다** —
`cardTheme` 제거(Material Card 사용처가 앱 전체에 1곳뿐인 죽은 설정이었다) + 그 1곳에서 직접 스타일링.
재발 방지로 `pubspec.yaml`에 "버전 의존 API 금지 + 회피 목록" 주석.

> **교훈: 다음 사이클엔 툴체인 버전을 먼저 맞춘다.** 코드 충돌보다 환경 충돌이 먼저 터졌다.
> (SDK 통일 자체는 Gradle/AGP까지 딸려오므로 **제출 후**에 한다.)

### 머지 순서 실행 결과 (07/30)

| 순서 | PR | 방식 | 결과 |
|---|---|---|---|
| 1 | app#9 seam | **merge commit** | ✅ app#10이 이 커밋 위에 쌓여 있어 squash하면 대충돌 → merge commit 필수였다 |
| 2 | ai#34 (#30 생성층) | squash | ✅ pytest 92 통과 |
| 3 | ai#35 (#22 디자인) | squash | ✅ 문서만 |
| 4 | app#10 (#7 일부) | merge commit | ✅ C7 발생 → app#11로 즉시 해소 |
| 5 | app#11 (C7 수정) | squash | ✅ analyze 0 / test 84 |

**dev 현재**: app `analyze 에러 0 · test 84` / ai `pytest 92` — 둘 다 초록.

---

## 2-1. 원래 예측했던 충돌 지점 (참고용 원문)

### 🔴 C1. v3 전반부 화면이 **두 벌** 존재 — 최우선

`app#8`의 `quest_journey_screen.dart`가 v3를 한 화면 상태머신으로 재현해
`setup · map · gps · summon · dialogue · quiz · order`를 **이미 포함**한다. `app#7`(jch) 범위와 정면 중복.

**W1 결정 = "정본 하나만 고르고 나머지는 파킹".** 전면 리팩터는 제출 후.
- 두 구현을 나란히 돌려보고 **데모에 쓸 쪽을 그 자리에서 정한다**(완성도 기준, 코드 예쁨 기준 ❌).
- 안 고른 쪽은 **삭제하지 말고 파일째 남긴다**(`_legacy` 접미사). 되돌릴 여지 확보.
- 제출 후: 오케스트레이터(상태·게이팅) + 개별 화면 위젯 구조로 정리.

### 🔴 C2. 혼불 레이더 중복
`honbul_home_screen.dart`(kys)와 `app#7`의 혼불 레이더 GPS가 겹침 → **위젯 1개로 통일**(`widgets/honbul_radar.dart`). 담당 jch.

### 🟡 C3. 앱 진입점 변경
`app#8`에서 로그인 후 첫 화면을 `MainShell`(5탭) → `HonbulHomeScreen`으로 바꿨다. **전원 합의 안건.**

### 🟡 C4. 스키마는 **계약 충돌**이 남는다
`ai#30`이 내리는 JSON 필드명·모양이 앱 기대와 어긋나면 **조용히 폴백**해서 빈 화면이 된다. 파일이 안 겹치니 git은 안 알려준다.
→ **계약 테스트로 잡는다**(3절 kys ⑤). `시나리오구조화.md` 6-1 노드1 JSON이 정본.

### 🟡 C5. 공용 파일 동시 수정
`theme.dart` · `widgets/*` · `schemas.py` → **추가만(append-only)**, 기존 값 변경은 PR 본문 명시.

### 🟢 C6. CI가 없다
네 레포 전부 워크플로 0건 → 깨진 코드가 그대로 dev에 들어간다. W1에 kys가 깐다.

---

## 3. W1 — 통합 + 기능 + 로컬 실동작 (08/01~08/07)

### 🟦 김예슬 (kys) — **통합 오너 + 로컬 실동작 오너** ⭐신규 담당

- **① 머지 순서 집행** (의존 낮은 것부터, 단계마다 통플레이 확인):
  1. `app#8 app-v3-back/kys/v1` — 디자인토큰·AR위젯·DTO seam (**남들이 기다리는 seam**)
  2. `ai#30 node-schema-gen/pjh/v1` — 생성층
  3. `ai#31 content-qa-design/ljs/v1` — 콘텐츠·힌트 문구
  4. `app#7 app-v3-front/jch/v1` — **C1 정본 결정과 함께**
- **② 충돌 해소 주재** — C1·C2 정리, C3~C5 합의 반영.
- **③ E2E 에러 정리** — 종로 1코스 처음~엔딩 통플레이 돌려 터지는 것 전부 잡고 이슈로 등록·배분.
- **④ CI 구축**(C6) — PR마다 자동:
  | 레포 | 검사 |
  |---|---|
  | app | `flutter analyze`(에러 0) + `flutter test` |
  | ai | `pytest` + `ruff` |
  | server | `npm run build` + `npm test` |
  목적 = **깨진 채로 dev에 못 들어오게.** 이게 있으면 "에러 확인"이 사람 일에서 기계 일로 넘어간다.
- **⑤ 계약 테스트**(C4) — AI 실제 응답 JSON을 앱 모델에 통과시키는 테스트. 6-1 노드1을 픽스처로 고정 → 어긋나면 CI가 빨개진다.
- **⑥ 🔥 로컬 풀스택 실동작** — *이번 주차의 종착점.*
  - `docker-compose.yml`의 **`server` 블록 주석 해제**(현재 `# TODO(정찬희/김예슬)` 상태 = 서버가 compose에 아예 안 붙어 있다). `dokkaebi-server` Dockerfile 작성.
  - `compose up` 한 방으로 **pg + redis + ai + server** 기동 → 앱이 **목/더미 아닌 실제 서버**를 때려서 처음~엔딩 완주.
  - 실동작 체크리스트: 게스트 로그인 → 시나리오 생성(실 TourAPI) → GPS 인증 → 대화(실 LLM) → 조각 수집 → 단서 체인 → 피날레 → 엔딩 → 재시작 후 진행 복원.
  - `dokkaebi-infra/README.md`에 **팀원이 그대로 따라 하면 뜨는** 실행 절차 갱신.
- **⑦ 도감·칭호** — 앱 화면(ljs 설계 기반) + 서버 API. 조각·단서·플래그가 이미 영속되므로 그 위에 얹는다.
- **브랜치:** `integration/kys/v1` · `ci/kys/v1` · `local-fullstack/kys/v1`

### 🟨 정찬희 (jch)
- **C1 정본 결정 참여** — 자기 화면과 kys 오케스트레이터를 나란히 돌려 데모 경로 확정.
- **C2** 혼불 레이더 위젯 1개로 통일.
- 자기 브랜치 머지 충돌 해소 + 통플레이 앱 버그 픽스.
- **서버 Dockerfile·환경변수** kys와 짝으로(⑥ 지원) — 서버 오너십 일부 인수(개발계획 M3 재조정).
- **W2 준비**: 파티 UI 와이어 확인(구현은 W2).
- **브랜치:** `app-v3-front/jch/v2`

### 🟧 박준형 (pjh)
- **생성층 실데이터 검증** — 종로 4노드가 스키마 v1.1 필드(motivation/strategy/actions/grants/requires/clue/hint)를 **빠짐없이** 채우는지 직접 확인. 빈 필드는 앱이 조용히 폴백하므로 화면만 봐선 모른다.
- **위시 기반 맞춤 생성**(앵커+샛길) — 설계 노트 있음, 구현 차례. **심사 핵심(데이터 활용) 서사라 W1에 남긴다.**
- **RAG 검색 품질** — `retrieve.py`에 "배선 완료, top-k·재랭킹·신뢰도·저신뢰 재검색은 TODO(박준형)"로 남아 있음. 채운다.
- 정답유출·어미·환각 자동체크를 **CI에 물릴 형태**로(kys와 인터페이스 합의).
- **브랜치:** `node-schema-gen/pjh/v2` · `wish-custom-gen/pjh/v1`

### 🟩 이지선 (ljs)
- **힌트 문구 3단 슬롯** — 종로 4노드 × H1/H2/H3. 타이밍 코드는 붙어 있어 **문구만 넣으면 즉시 동작**(`hint_ladder.H1/H2/H3`, `"slot"`은 미채움 인식).
- 종로 정답지 baseline 확정 + **LLM QA 4항목 리포트 1회**(발표자료 재료).
- **도감/단서함 카드 규격 전달** → kys ⑦이 이것 때문에 대기 중. **W1 전반부에 넘겨야 함.**
- 혼잡도 → 비인기지 **보상 가중치 밸런싱** 확정.
- **W2 준비**: 파티·랭킹 UI 설계.
- **브랜치:** `content-qa-design/ljs/v2`

**W1 DoD** — ☐ 4갈래 dev 머지 ☐ C1~C3 결정 ☐ CI 초록 ☐ **`compose up`으로 pg+redis+ai+server 기동, 앱이 실서버로 처음~엔딩 완주(영상)** ☐ 도감·칭호 표시 ☐ 맞춤생성 동작

---

## 4. W2 — 실시간 멀티 (08/08~08/14)

| 담당 | 할 일 |
|---|---|
| 김예슬 | Socket.io 룸·4인 파티·**조각 동기화·랭킹(Redis 원자처리: Lua/Sorted Set)** — `party.gateway.ts` 골격 채우기. 동시성 테스트(조각 중복 방지) |
| 정찬희 | 파티 UI — 초대·파티창·실시간 조각 공유·인게임 채팅 |
| 이지선 | 파티·랭킹 화면 디자인 마감, 멀티용 NPC 대사 톤 |
| 박준형 | 공용화 자동검수(규칙·텍스트 필터), eval 회귀셋 정리 |

**W2 DoD** — ☐ 4인 파티 합동 복원 클리어 ☐ 랭킹 반영 ☐ 조각 중복 0(동시성 테스트 통과)

---

## 5. W3 — 배포 (08/15~08/21)

### 배포 모델 결정 (근거)

**핫패스는 상주 컨테이너, 배치만 서버리스.** `아키텍처_파이프라인.md` 9절 결정 그대로.

| | 왜 |
|---|---|
| `ai`·`server` = **상주** | ① Socket.io 실시간 파티는 연결을 붙잡아야 함(Lambda 불가) ② `retrieve.py`가 지역 임베딩 인덱스를 **인메모리**로 들고 씀 ③ LLM 호출이 이미 수 초 → 콜드스타트 얹으면 첫 대화 체감 붕괴 |
| 데이터 배치 = **서버리스/스케줄** | TourAPI 수집·노드빌더·임베딩 인덱싱 — 간헐적·상태 없음 |

> "24시간 띄우는 게 아깝다"는 **서버리스로 갈 이유가 아니라 안 쓸 때 인스턴스를 내리면 되는 문제.**
> 심사·시연 기간만 켠다. 작은 VM(Lightsail·NCP micro) 월 몇천 원이면 재설계 공수보다 압도적으로 싸다.

| 담당 | 할 일 |
|---|---|
| 김예슬 | **단일 VM + compose 배포**(W1의 로컬 compose 재활용 → 하룻밤 수준), 도메인·HTTPS, 환경변수·시크릿 분리, 헬스체크·재시작 정책 |
| 정찬희 | 앱 **릴리스 빌드**(APK/테스트 기기 배포), 서버 주소 전환(로컬→배포), 실기기 확인 |
| 박준형 | 데이터 배치 스케줄 분리(수집·임베딩), LLM 비용·지연 측정 |
| 이지선 | 배포 환경 콘텐츠·에셋 최종 반영 |

**W3 DoD** — ☐ 외부 접속되는 서비스 ☐ 설치 빌드로 실기기 플레이 ☐ 안 쓸 때 내리는 절차 문서화

---

## 6. W4 — QA·제출물 (08/22~08/28)

| 담당 | 할 일 |
|---|---|
| **전원** | 종로 **현장** 통합 테스트(GPS 반경·AR·네트워크), 버그 픽스 |
| 김예슬 | 릴리스 관리(`dev`→`main`) + 태그, 최종 회귀, 성능(LLM 캐시·지연) |
| 정찬희 | **시연 영상**(솔로+멀티) 촬영·편집 |
| 박준형 | **데이터 활용·AI 파이프라인** 발표자료 (EDA·생성·RAG·eval 수치) |
| 이지선 | **제안서·발표자료** 반영, 스크린샷·에셋 정리 |

**W4 DoD** — ☐ 제출물 일체(시연영상·발표자료·제안서·빌드) ☐ `main` 릴리스 태그

---

## 7. 규칙

1. **W1엔 새 기능 브랜치를 따지 않는다** (1절에 남긴 것 외). 합치기가 먼저 — 충돌 제곱 방지.
2. **머지는 kys 순서대로.** 순서를 건너뛰면 seam 없이 합쳐져 되돌리기가 커진다.
3. **CI 초록 아니면 머지 금지** (W1 ④ 이후).
4. 공용 파일(`theme.dart`·`widgets/*`·`schemas.py`)은 **추가만**, 기존 값 변경 시 PR 본문 명시.
5. **C1은 "고르기"고 "리팩터"가 아니다.** W1에 구조 정리 들어가면 그 주는 끝난다.
6. **막히면 24시간 안에 말한다.** W1은 서로 물려 있어 한 명이 조용히 막히면 전원이 선다.
7. 브랜치 `<기능>/<이름>/<버전>`, `dev`로 PR, 1인 리뷰 승인 — 기존 유지.

---

## 8. 한 줄 요약

> **W1 = 합치고 + 기능 채우고 + 로컬에서 실서비스로 완주.** (배포 아님, 실동작까지)
> **W2 = 실시간 멀티. W3 = 배포. W4 = QA·제출물.** 9월 초 제출, 버퍼 1주.
> 통합 오너는 김예슬 — 머지 순서·충돌·E2E·CI·계약 테스트·로컬 풀스택.
> W1을 지키는 조건은 **1절의 컷 4개** — 특히 C1을 리팩터가 아니라 **정본 고르기**로 끝내는 것.
  