# 이번 주차 역할분담 — 시나리오 파이프라인 통합 마감 → 경로 분기·M3 진입

> 기준일: 2026-07-05 · 대상: `dokkaebi-ai` 시나리오 파이프라인
> 지난 주(06-23) 결과: **① 선택 단계 3개 hook 전부 dev 머지 완료** (위시·비인기·식음/예산)
> 이번 주 목표: **① 통합 검증·마감 → ② 경로 분기 착수 → M3(맞춤·공용화·밸런싱) 준비**
>
> 지난 주 문서는 git 히스토리에 있음(`WEEKLY-PLAN.md` @ 06-23).

---

## 0. 지금 어디까지 왔나 (스냅샷)

```
[입력: 좌표·반경·위시·예산·이동수단]
   ▼
① 선택 단계 (결정론)  ── build_route ─────────────────────────┐
   ✅ 위시 앵커      select_wishlist_anchors  (정찬희)         │  ← 3개 hook
   ✅ 비인기 앵커    select_lowtraffic_anchors(이지선/실데이터) │     전부 배선·머지 완료
   ✅ 식음·예산      interleave_food          (박준형/실+mock)  │
└──────────────────────────────────────────────────────────────┘
   ▼
② 생성 단계 (LLM · 노드별 병렬)   ← 모양 그대로. 유일 예외 = 경로 분기(이번 주 kys)
   ▼
③ 조립 _build_quest  ⚠️ 여기 통합 갭 1건 (아래 최우선)
```

**이번 주의 성격 = "각자 짠 걸 실제로 하나로 돌려보는 통합 주".**
지난 주는 각자 hook을 병렬로 채웠고, 이번 주는 **전 조건을 동시에 켜고 시나리오 1개가 끝까지 도는지**를 만든다.

---

## 1. 🔴 최우선 공동 작업 — 식음노드 기억석 오인 방지 (김예슬 + 박준형)

지난 주 §2에서 "공동 PR로 조율"이라 표시해둔 cross-talk가 **아직 미해결**이다.

- **증상:** `generator._build_quest`가 `route`의 **모든** 노드에 `fragment_id`를 붙임
  → 박준형 hook이 삽입한 `kind="food"/"cafe"` 노드가 **기억석 조각으로 오인**되고,
  `total`(조각 수)·타이틀 "N조각 코스"가 식음노드만큼 **부풀어남**.
- **해야 할 것:** ✅ **완료 (2026-07-07, `quest-foodnode/kys-pjh/v1` 커밋 `a7d2aa6`)**
  - [x] `generator`의 route 루프에서 **식음노드는 조각 카운트/`fragment_id`에서 제외** (`_plan_nodes`가 `kind in {"food","cafe"}` 분기)
  - [x] 식음노드는 별도 퀘스트 타입(경유/보상)으로 조립 (`_build_food_quest` — 대사·쿠폰만, 기억석 미션·fragment 없음)
  - [x] `total`·`fragment_id` 재계산이 **관광 노드 기준** (`stone_total` 필드 추가)
  - [x] 회귀 테스트: `test_foodnode.py` 8종 — 식음 ON일 때 조각=관광노드, 식음 OFF와 동일 (전체 35 통과)
- **PR:** 한 PR로 (`quest-foodnode/kys-pjh/v1`) — generator는 김예슬, 판정 규칙·마커는 박준형
- **경계:** 이거 끝나기 전엔 "전 조건 ON E2E"가 의미 없음 → **이번 주 1번으로 처리**

---

## 2. 사람별 분담

### 🟦 김예슬 (kys) — 통합 오너 + ② 경로 분기
- **위치:** ③ 조립 통합 + ②의 유일한 모양 변경
- **이번 주:**
  - [x] §1 식음노드 오인 방지 공동 PR (generator 측) — 완료
  - [x] **전 조건 ON E2E** (2026-07-07): 위시+비인기+식음+예산 전부 켜고 종로 시나리오 1개 생성 확인.
        결과: 노드 7(관광 5 + 식음 2), `stone_total=5`, **조각수=관광노드수 ✓**, 식음 `fragment_id` 전부 None ✓, 위시(경복궁) 반영 ✓.
        ⚠️ **비인기 앵커는 0개 반영** — config·density 실데이터(snapshot 6개)는 정상인데 **snapshot 지역명 ↔ TourAPI 노드명 매칭 실패** → 이지선 hook 확인 필요(§이지선 항목).
  - [x] 잡정리: [route_builder.py](app/scenario/route_builder.py) 주석 담당자 이름 정정 — 완료
  - [x] (계획 외) 동선 개선: 위시앵커 dist_m backfill + NN/2-opt 정렬 (`route-nn`), 위시 좌표·이름 버그픽스, 식음 dist_m
  - [ ] **경로 분기 착수**: `node_sequence` 선형 → 트리(선택 분기). `run_branching`(런타임 대화 분기)은 이미 배선됨 → 그 위에 **route 분기** 얹기. **eager(미리 생성) vs lazy(밟을 때 생성)** 결정이 핵심 ← **§1이 dev 올라가면 착수**
- **브랜치:** `quest-foodnode/kys-pjh/v1`(완료, 미푸시) → `route-branching/kys/v1`

### 🟩 이지선 (ljs) — 비인기 마감 + M3 밸런싱 데이터 선행
- **위치:** ① 비인기(완료) 검증 → M3 밸런싱 준비
- **이번 주:**
  - [x] 비인기 앵커 **통합 검증**: 컷오프(집중률/중심성)로 뽑힌 샛길이 실제 route에 끼는지, 실데이터 결측 시 폴백(`[]`) 정상인지
    - 🔴 **kys E2E(07-07)에서 발견**: `lowtraffic_anchors=2`로 켰는데 **실제 route에 비인기 앵커 0개**. config·snapshot(6개) 정상 → **snapshot 지역명 ↔ TourAPI 노드명 매칭이 안 됨**. 이름 정규화/매칭 키 점검 필요. (재현: `DOKKAEBI_SCENARIO_LOWTRAFFIC_ANCHORS=2`로 종로 시나리오 생성)
  - [x] 컷오프 튜닝: 종로 실분포 기준 임계값 재조정 (너무 많이/적게 뽑히지 않게)
  - [x] **(M3 선행)** 혼잡도 → 비인기지 **보상 가중치 밸런싱 데이터** 초안
  - [ ] **디자인:** 위시리스트·시나리오 라이브러리 UI 설계 착수 (M3용, 정찬희 구현 기반 제공)
- **브랜치:** `lowtraffic-tune/ljs/v1`
- **경계:** generator/route_builder 안 건드림

### 🟨 정찬희 (jch) — 위시 앵커 마감 + 앱 위시 입력 배선
- **위치:** ① 위시(복원 완료) 검증 → 앱 연결
- **이번 주:**
  - [ ] 위시 앵커 **반경 폴백 검증**: content_id 매칭·반경 밖 좌표 합성노드·반경 내 후보 0개 폴백이 실제로 도는지
  - [ ] **(M3 선행)** 앱: 위시리스트 입력 UX(검색/지도핀/태그) → 시나리오 생성 요청 배선 착수
  - [ ] 위시가 반경 밖일 때 사용자 경고 정책 확정 (경고 문구/거절 기준)
- **브랜치:** `wishlist-verify/jch/v1`
- **참고:** 지난 주 실수 revert는 복구 완료(dev 반영됨) — 현재 dev의 `wishlist.py`가 정상 구현본

### 🟧 박준형 (pjh) — 식음 실데이터 마감 + 오인방지 공동 + M3 맞춤생성 선행
- **위치:** ① 식음(완료) 실데이터화 + §1 공동 PR
- **이번 주:**
  - [ ] §1 식음노드 오인 방지 공동 PR (판정 규칙·`kind` 마커 측)
  - [ ] **mock → 실데이터 마감**: TourAPI `contentTypeId=39`/카카오 키로 종로 실측, `price_source="mock"` 잔존 최소화·경로 검증
  - [ ] 예산 게이팅 **경계 케이스** 테스트(예산 0/과다, 후보 0개, 밴드 결측)
  - [ ] **(M3 선행)** 위시리스트 기반 **맞춤 생성(앵커+샛길)** 설계 착수
- **브랜치:** `quest-foodnode/kys-pjh/v1`(공동) → `food-realdata/pjh/v1`

---

## 3. 의존 순서 & 충돌 방지

```
①  §1 식음노드 오인방지 (kys+pjh 공동 PR)  ──► dev  ──┬─► kys route-branching
                                                       ├─► ljs 비인기 튜닝 (병렬)
                                                       ├─► jch 위시 검증  (병렬)
                                                       └─► pjh 식음 실데이터 (병렬)
```

**규칙**
1. **§1 공동 PR을 이번 주 먼저** 머지 → 그래야 "전 조건 ON E2E"가 신뢰됨
2. ljs·jch·pjh는 각자 hook 파일만 수정. `generator.py`/`route_builder.py` **직접 수정 금지**(kys 통합 영역)
3. 유일 cross-talk = §1 (kys↔pjh) → 한 PR로 조율
4. 브랜치 규칙 유지: `<기능>/<이름>/<버전>`, main/dev 직접 수정 ❌, PR 1인 리뷰 승인

---

## 4. 한 줄 요약

> 지난 주 = 각자 hook을 병렬로 채운 주. **이번 주 = 하나로 합쳐 실제로 돌리는 통합 주.**
> 최우선 = 식음노드 기억석 오인 방지(kys+pjh) → 전 조건 ON E2E → kys 경로 분기 착수 / 나머지는 M3(맞춤·공용화·밸런싱) 선행.
