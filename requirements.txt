# [v1] dokkaebi-ai 의존성 (base-pipeline/kys/v1, 2026-06-10)
fastapi>=0.110
uvicorn[standard]>=0.29
pydantic>=2.6
pydantic-settings>=2.2
langgraph>=0.2
httpx>=0.27
numpy>=1.26        # 의미검색 인덱스(인메모리 코사인) — semantic-search/kys/v1, 2026-06-16
# 공유 캐시 백엔드 — DOKKAEBI_CACHE_BACKEND=redis 일 때만 사용한다.
# 미설치 시 core.cache가 memory로 조용히 폴백해 인스턴스별 캐시가 된다
# (.env.example은 compose·배포에서 redis를 안내). ai-logic-fix/pjh/v2, 2026-08-12
redis>=5.0
pytest
