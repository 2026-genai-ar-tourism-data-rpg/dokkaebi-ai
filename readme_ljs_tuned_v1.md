# 비인기지 보상 가중치 밸런싱 — EDA 초안 (M3 선행)

## 1. 작업 개요

상위 문서: [readme_ljs_v1.md](readme_ljs_v1.md) (비인기 앵커 선택 로직)
대상: `WEEKLY-PLAN.md` §2 이지선 — "(M3 선행) 혼잡도 → 비인기지 보상 가중치 밸런싱 데이터 초안"

`app/scenario/density.py`에 이미 존재하는 STUB:

```python
def reward_weight(tier: str) -> float:
    """비인기지 보상 가중치(분산 유도). 실수치는 EDA로 확정."""
    return 1.5 if tier == "low_traffic" else 1.0
```

이 함수는 현재 코드 어디에서도 호출되지 않는 orphan 상태다(실제 보상 시스템 자체가 아직 없음).
이 문서는 그 "1.5"라는 임의값의 근거를 종로 실측 데이터로 검토하고, M3에서 보상 로직을 붙일 때
참고할 밸런싱 초안을 제시한다. 아울러 같은 WEEKLY-PLAN §2 항목인 "컷오프 튜닝(종로 실분포 기준
임계값 재조정)"의 근거가 지금까지 커밋 메시지(`bf432ed`, `b4dc9c9`)와 코드 주석에만 흩어져 있어,
§5·§6에 그 근거를 노트로 정리했다. **코드 변경 없음 — 데이터 분석 문서만.**

---

## 2. 데이터 소스 및 방법론

- 소스: `fetch_density_snapshot_sync("종로")` — `TatsCnctrRateService`(집중률) 벌크 조회, 기존 비인기 앵커 선택 로직과 동일 snapshot
- 측정일: 2026-07-14
- 집계 단위: `tAtsNm`(장소명)별 `cnctrRate` 평균 — `density._avg_cnctr_rate`와 동일 계산

```text
concentration rows = 3,390
distinct 장소 = 113
```

---

## 3. avg_cnctrRate 분포 (113개 장소)

| 통계 | 값 |
|---|---:|
| min | 20.51 |
| p25 | 52.15 |
| median (p50) | 63.10 |
| p75 | 70.92 |
| max | 83.67 |
| mean | 60.41 |

10분위:

```text
40.44 / 50.27 / 54.12 / 58.29 / 63.10 / 65.05 / 67.49 / 73.06 / 76.40
```

참고: 현재 `density_lowtraffic_max_avg_rate = 70.9`(`bf432ed`)는 이 분포의 **p75**에 해당한다.
즉 지금 "비인기 후보"로 인정되는 절대 컷오프 자체가 이미 "상위 75% 이내(안 혼잡한 쪽)"를 의미하고,
그 안에서 근처 후보 중 `avg_cnctrRate`가 가장 낮은 것부터 뽑는 구조다.

---

## 4. 실제 라우팅 샘플 관찰 (종로 관광특구 인근 7노드)

`readme_ljs_v1.md` §11의 동일 샘플로 `select_lowtraffic_anchors(nodes, k=2)` 실행 결과:

| 노드 | avg_cnctrRate | 비고 |
|---|---:|---|
| 독립선언문 배부 터 | 매칭 없음 | — |
| 쌈지길 | 79.67 | p75 초과 → 컷오프 탈락 |
| 서울 운현궁 | 83.17 | p75 초과 → 컷오프 탈락 |
| 필소굿캘리 | 매칭 없음 | — |
| 안녕인사동 | 82.27 | p75 초과 → 컷오프 탈락 |
| 서울 우정총국 | 64.77 | median 근처 → **선택됨** |
| 탑골공원 팔각정 | 73.93 | p75 초과 → 컷오프 탈락 |

k=2로 설정했지만 컷오프를 통과한 후보가 1개뿐이라 **실제 선택은 1개**(`서울 우정총국`).
인사동 관광특구 도보권은 대부분 p75(70.9)를 넘는 혼잡 구역이라, 이 지역에서는
"비인기 앵커"가 자주 나오지 않는 게 정상 동작이라는 것을 확인.

---

## 5. 컷오프 임계값 근거 — `density_lowtraffic_max_avg_rate`가 median이 아니라 p75인 이유

`bf432ed` 커밋 메시지에 있던 근거("median은 안국역 인근에서 0개")를 실제 API로 재현.

측정: 안국역 인근(126.9858, 37.5765) 반경 800m, `location_based_list`로 얻은 실제 후보 20개에
`select_lowtraffic_anchors(nodes, k=2)`를 median(63.1)과 현재값 p75(70.9)로 각각 적용.

| 컷오프 | 통과 후보 수 | 선택 결과 |
|---|---:|---|
| median (63.1) | 0 | (없음) |
| p75 (70.9, 현재값) | 1 | 서울 우정총국 |

안국역 인근(운현궁·인사동·북촌 도보권)은 종로에서도 가장 혼잡한 축에 속해서, median을 컷오프로
쓰면 이 지역에서는 "비인기 앵커"가 구조적으로 하나도 안 나온다. 반대로 컷오프를 너무 완화하면
컷오프 자체가 무의미해지므로, "median보다 위(더 혼잡한 쪽)까지는 허용하되 상한은 둔다"는 절충으로
p75(70.9)를 택함. §3의 분포 기준으로는 상위 75% 구간(안 혼잡한 쪽 3/4)까지를 "비인기 후보 가능"
범위로 인정하는 셈이다.

---

## 6. hub_rank 미매칭 정렬 버그 근거 (`b4dc9c9`)

`_rank_hub_rows`가 붙이는 `_local_rank`가 없는 노드(hub 목록에 아예 없는 곳, 즉 `hub_rank=None`)를
정렬 키에서 어떻게 다루느냐의 문제.

```text
정렬 키 = (avg_rate, -hub_rank 관련 항, dist_m)  # 오름차순 정렬 → 값이 작을수록 먼저 뽑힘

구버전: -(hub_rank or 0)
  hub_rank=None  → -(None or 0) = -0 = 0
  hub_rank=5     → -(5) = -5
  → -5 < 0 이므로 "hub 목록에 순위 5로 올라있는(=어느 정도 유명한) 후보"가
    "hub 목록에 아예 없는(=진짜 무명인) 후보"보다 먼저 뽑힘 → 의도와 반대

신버전: hub_rank is None이면 -inf, 아니면 -hub_rank
  hub_rank=None  → -inf
  hub_rank=5     → -5
  → -inf < -5 이므로 진짜 무명 후보가 먼저 뽑힘 → 의도대로
```

avg_rate가 동률인 두 후보가 있을 때만 드러나는 정렬 순서 문제라 단위 테스트로 놓치기 쉬웠고,
`tests/scenario/test_lowtraffic.py`에도 이 동률 케이스를 직접 겨냥한 테스트는 아직 없음(현재는
hub_rank 카테고리 필터링 테스트만 있음) — 필요하면 회귀 테스트 추가를 후속으로 남겨둔다.

---

## 7. 보상 가중치 밸런싱 초안

`avg_cnctrRate`를 4구간(quartile)으로 나눠 가중치를 제안한다. 현재 `reward_weight()`는
tier 이진값(`low_traffic`/`popular`)만 받으므로, 이 표는 **향후 연속·다단계 가중치로 확장할 때**의
초안이며 지금 당장 코드에 반영하는 것은 아니다.

| 구간 | avg_cnctrRate 범위 | 라벨(제안) | 가중치(제안) |
|---|---|---|---:|
| Q1 | ≤ 52.15 (p25 이하) | 매우 한산 | 2.0 |
| Q2 | 52.15 ~ 63.10 | 한산 | 1.6 |
| Q3 | 63.10 ~ 70.92 | 보통 | 1.3 |
| Q4 | > 70.92 (p75 초과) | 혼잡 | 1.0 (= popular) |

근거:

```text
- Q4 하한(70.92)은 현재 density_lowtraffic_max_avg_rate와 동일 → 지금의 "popular"
  판정과 정합성을 유지.
- 현재 STUB 1.5는 Q2~Q3 사이에 위치 → "low_traffic" 태그가 Q1~Q3를 뭉뚱그린 이진값이라는 점을
  감안하면 크게 벗어난 값은 아니지만, Q1(매우 한산)과 Q3(컷오프 근처)를 동일하게 1.5로 취급하는 건
  "분산 유도"라는 목적에는 다소 느슨함.
- 가중치 값(2.0/1.6/1.3/1.0) 자체는 이번 표본(113개, 단일 시점) 기준 제안치이며,
  실제 보상 정책(포인트/코인 단위) 확정 시 M3에서 재조정 필요.
```

---

## 8. 연속형 확장 옵션 (참고, 미구현)

이산 tier 대신 연속 함수로 갈 경우의 방향만 기록한다(구현 아님):

```text
weight(avg_rate) = 1.0 + max(0, (max_avg_rate - avg_rate) / (max_avg_rate - min_avg_rate)) * (W_max - 1.0)

- max_avg_rate = density_lowtraffic_max_avg_rate(70.9, 현재 popular/low_traffic 경계)
- min_avg_rate = 분포 min(이번 표본 20.51) 또는 p5 등 완충값
- W_max = 정책으로 결정할 최대 가중치(예: 2.0)
```

전제 조건: `select_lowtraffic_anchors`가 선택 시점에만 알고 있는 `avg_rate`를 node에
남겨야 함(예: `node["cnctr_rate_avg"] = avg_rate`). 이건 `density.py` 안에서 가능하지만,
그 값을 실제로 소비하는 보상 로직은 generator 쪽 영역이라 **김예슬(kys) 통합 시점에 조율 필요**.

---

## 9. 한계

```text
- 표본 113개 장소, 단일 시점(2026-07-14) 스냅샷 — 계절/요일 변동 미반영
- 종로 1개 지역 기준. 타 지역 확장 시 분포 재산정 필요
- concentration row 없는 장소(신규/소규모)는 이번 EDA에서 제외됨 → 실제 매칭 커버리지의 일부만 반영
- 가중치 절대값은 보상 포인트/코인 스케일이 정해지지 않은 상태의 상대 배율 제안일 뿐
```

---

## 10. 쿠폰 밸런싱 데이터 (이슈 #31, 2026-07-31 추가)

대상: 이슈 #31 "보상 밸런싱 데이터(비인기·쿠폰) 갱신". §1~9는 비인기 앵커 가중치, 이 절부터는
쿠폰(식음 상권 보상) 파트다. **코드 변경 없음 — 데이터/근거만.**

### 10-1. 현재 코드 STUB
`app/tourapi/food.py`(박준형 담당)의 `interleave_food()`:
```python
node = {**choice, "coupon": {"to_kind": "food", "amount": 500}}   # 상권 쿠폰(mock 유지)
```
`price_band`(1~4, `BAND_LABEL = {1:"₩",2:"₩₩",3:"₩₩₩",4:"₩₩₩₩"}`)를 이미 계산해 놓고도,
쿠폰 금액은 밴드·구간과 무관하게 **항상 500원 고정**이다 — `reward_weight()`가 `low_traffic`
이진값만 보던 것과 같은 패턴의 STUB. 밴드 경계는 `config.py`에 실제로 정의돼 있다(1인 예산 기준):
```python
food_band1_max_krw = 10_000   # 미만 = ₩
food_band2_max_krw = 30_000   # 미만 = ₩₩
food_band3_max_krw = 60_000   # 미만 = ₩₩₩, 이상 = ₩₩₩₩
```

### 10-2. 정답지 실측값과의 격차
`docs/jongno-baseline-v1.1.md`(4노드 정답지) 기준 실제 쿠폰 흐름. 밴드는 §10-1 경계값으로
직접 계산(익선동 쌍화차 5,000원 · 인사동 지출 8,000원 — 둘 다 10,000원 미만 → **둘 다 밴드 1**):

| 구간 | 대상 노드 실제 밴드 | 코드 STUB | 정답지 실측 | 배율 |
|---|---|---:|---:|---:|
| 운현궁 → 익선동 | 밴드 1(5,000원) | 500원 | 500원 | 1.0x (일치) |
| 익선동 → 인사동 | 밴드 1(8,000원) | 500원 | 1,000원 | 2.0x |

**두 구간 다 같은 밴드(1)인데 쿠폰은 500→1,000으로 2배가 됐다 — 밴드 차이로는 설명이 안 된다.**
(이전 초안에서 "익선동=밴드2"로 잘못 가정해 밴드 연동으로 설명하려 했던 걸 §10-1 실제 임계값으로
재확인하며 바로잡음.) 여기에 퀴즈 정답 보너스(운현궁 +200원, 익선동 +300원)가 별도로 더 쌓인다.

### 10-3. 결론 — 밴드 연동이 아니라 "진행 보너스"가 실제 동인
§10-2로 밴드 연동 가설은 기각된다. 두 실측 데이터포인트가 가리키는 건 **밴드가 아니라 노드
순번**이다 — 운현궁→익선동(1번째 전이)은 500원, 익선동→인사동(2번째 전이)은 1,000원. 표본이
2개뿐이라 함수 형태(선형/배수 등)까지는 못 정하지만, 방향은 명확하다.

```text
coupon(order) = base * multiplier^(order - 1)   # 예: base=500, multiplier=2.0 → 500 → 1,000
```
이건 §10-4의 "이탈 방지 페이싱" 해석과 같은 방향이며, `price_band`는 **밴드 다양성이 실제로 있는
경우에만**(예: 다른 지역에서 밴드1과 밴드3 노드가 섞였을 때) 별도로 고려할 보조 축으로 격하한다 —
지금 종로 4노드 표본에서는 밴드가 전부 1이라 이번 데이터로는 검증 불가.

### 10-4. 정답지의 "진행 보너스" 해석
운현궁→익선동→인사동으로 갈수록 쿠폰이 커지는 건, **정답지가 의도적으로 뒤로 갈수록 보상을 키워
이탈을 막으려는 페이싱 설계**로 보인다(중반 이탈 방지 — `시나리오_구조화.md` 3-1절 이탈대응 취지와
같은 방향). 코드에 반영하려면 §10-3의 순번 기반 배율(`base * multiplier^(order-1)`)이 필요하고,
`base`·`multiplier` 값은 정책으로 확정해야 함 — **pjh 확인 요청 사항으로 별도 전달 권장**
(이 문서는 데이터 근거까지만, 코드 반영은 안 함).

### 10-5. 상한 제약 (정책 노트)
쿠폰이 실제 결제액을 넘으면 안 된다는 제약은 코드에 명시적으로 없다. 정답지 인사동 구간
(8,000원 지출 − 1,000원 쿠폰)은 쿠폰이 지출의 12.5%로 안전하게 낮다. §10-3 배율식을 실제
정책에 반영할 때도 "쿠폰 ≤ 목표 밴드 하한가의 일정 비율(예: 밴드1 하한 근처 기준 10~15%)"
같은 상한 규칙을 같이 넣는 걸 제안한다 — 정확한 밴드별 평균 단가는 `google_places.py`의
실측 데이터가 필요(이번 문서 범위 밖).

### 10-6. 한계
```text
- 정답지 표본이 2개 전이(운현궁→익선동, 익선동→인사동)뿐이라 순번 배율의 함수 형태(선형/기하 등)
  확정에는 부족 — 최소 1개 구간 더 필요(예: 인사동 이후 식음 노드가 추가되면 3번째 표본 확보)
- 두 표본 다 밴드 1이라 "밴드 다양성이 있을 때 밴드가 실제로 얼마나 영향을 주는지"는 이번
  데이터로 검증 불가(§10-3)
- 밴드별 실제 평균 단가 데이터 미확보(§10-5) — google_places 실측 필요
```
